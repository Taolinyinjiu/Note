int a[10]={ }; 是正确的数组初始化方式。使用空的初始化列表会将数组的所有元素初始化为0。
 int a[]={0}; 也是正确的。当使用初始化列表时可以省略数组大小，编译器会根据初始化列表的元素个数自动确定数组大小，这里会创建一个只包含一个元素0的数组。

 12+12+12+12+4

 数组和链表在随机访问元素时的性能差异很大。
 
 D选项"返回随机节点"是数组的优势,因为:
- 数组是连续存储的,可以通过下标直接访问任意位置的元素,时间复杂度为O(1)
- 链表必须从头结点开始遍历才能找到指定位置的节点,时间复杂度为O(n)

行优先读取更快的原因是:
1. 计算机内存是按行存储二维数组的,相邻元素在物理内存中连续存储
2. 行优先访问符合CPU缓存的预取机制,可以充分利用空间局部性
3. 每次读取可以最大化利用缓存行(cache line),减少缓存未命中的次数

D选项 int D[][8]:
- 这是C语言允许的简化写法
- 第一维可以省略不写,但第二维必须明确指定为8
- 编译器会将其处理为指向int[8]数组的指针

